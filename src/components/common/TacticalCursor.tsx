"use client";

// TacticalCursor is intentionally disabled — custom cursors read as "AI-generated UI".
// The file is kept for reference. To re-enable: restore the component body below.
export function TacticalCursor() {
  return null;
}
